# graphify-out/

This directory is generated and owned by Graphify — do not hand-edit files
here. Regenerate after any session that changes source code, and query it
before opening/editing/grepping any file in the repo (see `AGENTS.md` →
"Gate 1").

## Confirmed

- Main output file: **`graph.json`** — an AST-derived dependency graph of
  the entire Medusa backend + Next.js storefront.

## Still to confirm

`<FILL IN: exact CLI commands for querying and regenerating graph.json —
add them to AGENTS.md's Gate 1 section once known, not just here, since
that's the file every session actually reads first.>`
